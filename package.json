{
  "name": "linkforge",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "concurrently --kill-others --names API,WEB \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm start --prefix server",
    "test": "npm test --prefix server",
    "build": "npm run build --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
